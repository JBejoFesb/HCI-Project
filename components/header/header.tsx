import React from "react";
import styles from "./header.module.css";

const Header = () => {
    return(
            <div className=" bg-slate-600 text-right">Header Test</div>
    );
}

export default Header;